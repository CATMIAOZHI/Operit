# 上游 v1.12.2 吸收候选审查

## 当前状态与恢复

- 状态：实现、Lint 与独立复审已收口，已提交推送并开 PR（目标个人版 `personal/dev`）：分支 `codex/upstream-v1.12.2-sync`（每次提交都已推送到远端，不复述 HEAD 提交号以免失效），PR https://github.com/CATMIAOZHI/Operit/pull/60 。PR 的整批只读复审按轮记录在本文档「第七轮」起的小节里：**节号即轮次；每一轮的小节正文首句都写明该轮复审的对象提交号，判为 `AUDIT: CLEAN` 的轮次还在小节标题里写出了 CLEAN**；已记录的每一轮都是 0 个 P0 / 0 个 P1，其中「第十五轮」（第九轮复审，对象 `0a72fd4a5`）结论为 CLEAN，提出的问题均已处置并留痕，代码修复后都重跑了编译 + 整套单测 + Lint；第十六轮起因 CodeQL 报告 1 条新增高危 `java/zipslip` 告警而做了代码修复（对象 `babf69625`），该轮审计为 0 个 P0 / 0 个 P1 / 1 个文档级 P2（已处置），CodeQL 复跑后告警 #67 已 `fixed`、PR 的 CodeQL 检查转为 `pass`。是否合并由用户决定，2026-09-19。
- 用户授权：完成个人版 v1.12.2 适配；允许使用子代理（先后为 `gpt-6-astra` / `medium`，后续改为 `command-code/deepseek-deepseek-v4.1-flash` / high）；**明确禁止真机验证**；随后明确授权「提交推送、开 PR（目标个人版 dev）、由独立审计审计 PR」。仍未授权发布、打标签或合并。
- 实施工作树：`C:/Users/CAT/Documents/workspace/operit/.worktrees/upstream-v1.12.2`，分支 `codex/upstream-v1.12.2-sync`，基点 `4df70cea0`。主仓库的初筛副本不更新，以此工作树台账为准。
- 以下原筛选表的“未实施”说明是初始结论；实时状态以本节为准。

### 实施进度

- 第二轮进度：F01/F02/F03/F04/F05/F06、工具历史配对、语音档案、F07/F08、群聊首消息复用均已独立静态审计 CLEAN。
- 第三轮（ToolPkg）与第四轮（工具历史配对复审、记忆重建、摘要分区、APK 逆向）的审计结论见下文对应小节；下表初次筛选的“尚未实施”状态不代表实时状态。
- 语音档案读取旧配置完成一次迁移，之后以档案为唯一运行时真源；工厂按完整档案失效缓存。设置切换按档案 ID 重置输入，选择在事务内验证目标，保留个人 HTTP TTS 编辑器。
- F07 保留个人窗口宽度/固定scale/桌宠/bring-into-view豁免；加入有限尺寸、持久化熔断和初始化 ready 门禁。
- F08 引入输入 echo guard/FIFO，修正清队列后迟到回调误释放新任务的问题，生成源同步。群聊首成员复用已落库 ChatMessage，按 user+timestamp 过滤请求历史。
- 过程日志：`build-upstream-focused.log`（48 项，36 通过、12 因 Android `Log` 未模拟失败）、`build-upstream-speech-input.log`（31 项，29 通过、2 失败）暴露的三个缺口（Android `Log` 未模拟、重排属性图片链接、Responses 工具图片数组）均已修复，并被后续聚焦单测与整套单测覆盖；这两个日志不再是当前验证依据。
- F03 分批 IN（900 timestamps/批，排序去重）；F02 TXT/HTML 分页 hydration + Writer 输出，保留 folder 路径与选中变体，取消清理失败文件。
- F02/F03 独立审计 `audit_export`：CLEAN；确认长期读事务会使其他 DB 写入等待，内存仍至少需容纳一页正文/变体。
- F04 Web MIME；F05 description 改 `ConcurrentHashMap`；F06 仅缺失 key 时才补默认语音提示；Responses video 转换与上游测试。均已实施。
- F01 Room 设置入口先私有暂存后退出；沿用启动 PENDING 门禁，operation=ROOM_RESTORE；文件组 journal 回滚 db/wal/shm 和统计 spool，保留原 IDLE/COMPLETED，不登记 raw generation。独立审计 `audit_restore` 后纳入 CLEAN 集合。
- 首次 `:app:compileDebugKotlin` 成功（4m25s，`build-upstream-first.log`），仅作过程记录，不作为最终差异的验证。本地 JDK21/SDK36，terminal 已初始化，libs/local.properties/ripgrep 已从主工作树复制（忽略输入，不提交）。
- 未运行真机、adb、仪器测试。未提交、未推送。

#### ToolPkg 批次（第三轮，2026-09-19）

- 从 v1.12.2 取入整套 ToolPkg 能力：`ToolPkgApiVersion`、`ToolPkgManifestRequirement`、`ToolPkgLoadOrder`、`JsToolPkgApiRuntime`、`ChatRuntimeHookRegistry`、`ChatMessageMenuItemRegistry`、`ToolPkgChatRuntimeHookBridge`、`ToolPkgChatMessageMenuItemBridge`、`JsInitRuntimeScriptBuilder`、`JsExecutionScriptBuilder`、`JsToolManager`、`ToolPkgLoader`、`ToolboxPlugin`、`ToolPkgAiProviderRegistry`、`ToolPkgChatViewHookBridge`、`ToolPkgToolLifecycleBridge`、`ChatRuntimeHolder`。
- 手工合并（有意排除在线市场来源、旧 `chatMessageHooks`、`ToolPkgRuntimeMonitor` 与性能面板）：`ToolPkgParser`、`JsToolPkgRegistration`、`ToolPkgMainRegistrationScriptParser`、`ToolPkgCommonPluginConstants`、`PackageManagerToolPkgFacade`、`PackageManager`、`ToolPkgManager`、`ToolPkgCommonBridgePlugin`、`ToolPkgHookBridgeSupport`、`JsEngine`、`JsTools`、`ToolRegistration`（新增 `call_chat_model`）、`ToolResultDataClasses`（`ChatCallResultData`/`ChatCallTurnInfo`）、`ToolPackage`、`StandardChatManagerTool.callChatModel`、`EnhancedAIService.callFunctionModel`。
- `ToolPkgApiVersion` 的 Operit 版本正则接受个人版 `-ry.N` 与 `-dev(.N)` 后缀；API 1.0.1 门槛保持 `1.12.1+4`，因此个人版 `1.12.2-ry.1` 判定为支持 1.0.1，默认仍兼容旧包 1.0.0。
- 接线点已确认：`ToolPkgCommonBridgePlugin` 注册 ChatView / ChatMessageMenuItem / ChatRuntime / ToolLifecycle 四个桥；`ToolPkgParser`、`JsEngine` 调用 `ToolPkgApiCompatibility.requireSupported`；`PackageManager`、`ToolPkgManager` 使用 `ToolPkgLoadOrderResolver`。
- UI：`ToolPkgComposeDslScreen.kt` 取上游最终版，并把个人的 `acquireToolPkgExecutionEngine` 两处调用改回 `getToolPkgExecutionEngine`（个人 `ToolPkgManager` 无租约变体）；`ChatArea.kt` 接消息菜单与 `ToolPkgComposeDslDialogHost`；`AIChatScreen.kt` 增加 `embedded` 参数；`PackageManagerScreen.kt` 调用 `updateToolPkgPluginOrder`。
- 类型声明：`examples/types` 的 `chat.d.ts`、`compose-dsl.d.ts`、`index.d.ts`、`tool-types.d.ts` 取上游最终版；补 `core.d.ts` 的 `registerToolPkgChatMessageMenuItem` / `registerToolPkgChatRuntimeHook`，补 `toolpkg.d.ts` 的消息菜单与运行态 hook 类型（含 `ComposeDslScreenRef`、`ChatMessageSnapshot`、新增事件名），补 `results.d.ts` 的 `ChatCallResultData` / `ChatCallFinishReason`。有意不引入旧 `registerToolPkgChatMessageHook` / `message_persisted`，也不引入未采用的 `ModelConfigConnectionTestOutcome`（`index.d.ts` 相应去掉该别名）。
- 生成器侧：`tools/compose_dsl/templates/ToolPkgComposeDslGeneratedRenderers.kt.tpl` 与产物 `ToolPkgComposeDslGeneratedRenderers.kt` 均与 v1.12.2 逐字节一致；本机 Gradle 缓存缺少 Compose 源码 jar，`generate_compose_dsl_artifacts.py` 无法重跑，因此没有做重新生成验证。
- 版本：`versionCode = 100204`，`versionName = "1.12.2-ry.1"`；DEV 后缀仍由构建脚本追加。
- 验证证据：`:app:compileDebugKotlin` 成功（`build-upstream-plugin3.log`）；`:app:compileDebugUnitTestKotlin` 成功（`build-upstream-unit-tests.log`）；聚焦单测两轮共 173 项全部通过（`build-upstream-toolpkg-tests.log` 49 项、`build-upstream-toolpkg-tests2.log` 124 项），其中包含本轮新取入的 `JsRuntimeToolCallTest`、`ToolPkgApiCompatibilityTest`、`ToolPkgManifestSelectionTest`、`ComposeDslFilePickerRequestTest`、`MediaLinkParserFileTest`。
- `examples/types` 用 tsc 5.4.5 单独类型检查（`dts-check.log` / `dts-check-upstream.log`）：本工作树 **266** 条 vs 上游 v1.12.2 **267** 条，无新增错误（少的一条是 `toolpkg.d.ts` 的 `variantCount`，与本批排除项相关）；按（文件、行号、规则）比对一致，但 `java-bridge.d.ts` 有 9 条报文文本不同——该文件本身是个人版内容（非本批引入）。这些错误在本仓库 tsconfig 的 `skipLibCheck` 下不进入正常构建。另补两处与运行时不符的声明：`network.d.ts` 的 `browserTakeScreenshot`（运行时 `JsTools.kt` 早已提供）、`chat.d.ts` 里 `ChatCallOptions.recordTokenUsage` 的个人版限制说明。
- 本轮实际新增测试文件（相对基点，共 17 个）：`ChatMemoryRebuildTimeScopeTest`、`ChatMemoryWindowPlannerTest`、`DeepseekProviderMediaRoleTest`、`OpenAIResponsesVideoInputTest`、`OpenAiToolCallHistoryTest`、`StructuredToolCallBridgeHistoryTest`、`AIMessageManagerDialogueReviewTest`、`FunctionalPromptsSummaryTest`、`JsRuntimeToolCallTest`、`JsToolPkgRegistrationBridgeTest`、`ToolPkgApiDeclarationTest`、`ToolPkgManifestSelectionTest`、`ToolPkgOperitVersionSuffixTest`、`RoomRestoreFileSetTest`、`ComposeDslFilePickerRequestTest`、`ComposeDslTextFieldEchoGuardTest`、`ComposeDslTextInputDispatchQueueTest`。
- 上述 17 个的分类（整批审计逐文件与上游 blob 比对得出）：**3 个是本工作树新建**（`JsToolPkgRegistrationBridgeTest`、`ToolPkgOperitVersionSuffixTest`、`RoomRestoreFileSetTest`）；**10 个与上游 v1.12.2 逐字节一致**；**4 个上游已存在但按个人版适配**（`DeepseekProviderMediaRoleTest`、`OpenAiToolCallHistoryTest`、`StructuredToolCallBridgeHistoryTest`、`ComposeDslTextInputDispatchQueueTest`）。`MediaLinkParserFileTest` 个人版已存在且内容一致（仅行尾差异），不计入新增。

#### 独立审计与修复（ToolPkg 批次）

两个独立只读审计（`command-code/deepseek-deepseek-v4.1-flash` / high）分别审内核桥接与 Compose DSL/UI/类型声明，结论均为 `AUDIT: ISSUES`。

- 内核审计：0 个 P0、1 个 P1、8 个 P2。P1 = Hook 链缺超时预算（发送路径最长可卡 30 分钟且无提示）；该缺口在个人版基点就存在（上游在 v1.12.1 之前已修），本批取入上游 hook 桥时漏掉了配套预算。
- UI 审计：1 个 P0、2 个 P1、5 个 P2。P0 = 「整份取上游最终版」抹掉了个人版 `ToolPkgComposeDslScreen.kt` 的根节点 `onResume`/`onPause` 分发，会让阅读伴侣界面切回时不再 `activate`、离开时不再 `pause`；P1 = 初始 `onLoad` 不再等「当前屏 + 已 resume」；P1 = 工具屏渲染选项丢失 `moduleSpec`/`state`/`memo`。

已修复：

- 恢复 `ToolPkgComposeDslScreen.kt` 的个人版生命周期行为：`onResume`/`onPause` 分发（`rememberUpdatedState` + `LocalLifecycleOwner` + `LifecycleEventObserver` + `DisposableEffect` + `LaunchedEffect(isCurrentScreen, resumed)`）、`onLoad` 的 `isCurrentScreen && resumed` 双门禁、工具屏渲染调用补回 `moduleSpec`/`state`/`memo`；仅改工具屏入口，对话框宿主与动作派发路径保持上游形态。
- 引入上游 Hook 执行预算：新增 `ToolPkgHookExecutionBudget.kt`；`DisplayPreferencesManager` 新增 `toolpkg_hook_timeout_seconds`（默认 10 秒，clamp 1..60，含 getter 与 reset）；`PackageManager` / `PackageManagerToolPkgFacade.runToolPkgMainHook` 新增 `timeoutMillis`，Facade 换算为 `executeScriptFunction` 的 `timeoutSec`（向上取整到秒，最少 1 秒，因为个人版 `JsEngine` 只有整秒粒度）；`ToolPkgChatInputHookBridge` / `ToolPkgPromptHookBridge` / `ToolPkgSummaryHookBridge` 每条链共享同一 deadline，预算耗尽或单次超时即中断整条链。个人版 `PromptHookContext` 没有上游的 `onHookTimeout` 回调，因此这两处只记日志、不弹浮动提示。
- 发送路径提示：`ChatInputHookResult` 新增 `noticeMessage`，`ChatInputHookRegistry.dispatchSubmitRequested` 聚合，`AIChatScreen` **三处**提交路径（队列项发送、`steerPendingQueueMessage` 转向、普通发送；第三处是个人版独有路径，上游 v1.12.2 没有该函数）在 `when` 之后展示；新增字符串 `toolpkg_hook_timeout_continue_sending_with_plugin`（zh/en）。
- 超时判定收紧：`ToolPkgHookExecutionBudget.logTimeoutIfPresent` 只匹配引擎自身文案 `Script execution timed out after`（`JsEngine` / `JsExecutionScriptBuilder` / `JsToolManager` 三处产出均含该串），不再用宽松的 `timed out` 子串，避免把插件自身的超时错误误判为预算超时并提前中断整条 hook 链。
- 清理排除项残留：删除 `PackageManager.peekInstance()`（性能采样伴生代码）与 `ToolPkgManager` 上残留的「性能分析界面」KDoc。
- 版本解析健壮性：`OperitVersion` 正则放宽为只取前导 `major.minor.patch` 与可选 `+build`，其余后缀（`-ry.N`、`-dev`、未来频道标记）一律忽略，避免新增后缀时让所有 ToolPkg 加载与 JS 执行抛异常；无法解析的版本仍拒绝。
- 补测试：`ToolPkgOperitVersionSuffixTest`（后缀容忍、`1.12.1-ry.3` 低于门槛、非法版本拒绝）、`JsToolPkgRegistrationBridgeTest`（桥上 1.0.1 版本门禁声明、注册会话捕获 `chatRuntimeHooks`）。上游 `JsToolPkgRegistrationTest` 另外 3 个用例依赖市场来源能力，个人版未采用，故未移植。

经评估后**有意不改**（记录决定，非遗漏）：

- `toolpkg_hook_timeout_seconds` 没有设置界面：上游在 `GlobalDisplaySettingsScreen` 有 1–60 秒滑块与 4 条字符串，个人版本轮只接偏好与消费方，固定默认 10 秒；若要用户可调需另开小批次。
- prompt / summary 两个桥超时后只有 `AppLogger` 日志、没有浮动提示：个人版 `PromptHookContext` 没有上游的 `onHookTimeout` 字段（全仓零命中），上游那两行调用在本地无法编译；完整对齐需给 `PromptHookContext` 加字段并在 `MessageProcessingDelegate` 接线。
- 没有显式预算的 hook 路径：`ToolPkgChatRuntimeHookBridge.onEvent`、`ToolPkgChatMessageMenuItemBridge.dispatchClick`（这两个文件是本批**新增**的，即新引入的未受预算路径，上游 v1.12.2 同样未加预算）、`ToolPkgCommonBridgePlugin` 的 xml-render / input-menu / message-processing（基点已有，`git show 4df70cea0:` 该文件已有 4 处 `runToolPkgMainHook`）、`ToolPkgChatViewHookBridge`、`ToolPkgToolLifecycleBridge`、`ToolboxPlugin` 的 app lifecycle hook、`ToolPkgJsAiProviderService`、以及 navigation entry action 的内部转调。它们统一走 `PackageManagerToolPkgFacade` 的 `timeoutMillis → timeoutSec` 折算，**保留引擎默认上限**（`JsTimeoutConfig.MAIN_TIMEOUT_SECONDS = 1800`，即 30 分钟）且无用户提示；若日后要收口，做法与 ChatInput/Prompt/Summary 三桥一致（传 `ToolPkgHookExecutionBudget.create().remainingMillis()`）。
- 引擎释放没有租约计数（与个人版基点同形）：`ToolPkgManager` 是「身份匹配即移除并 destroy」，上游多了 `activeLeases > 1` 减计数分支；由于 `executionContextKey` 含每路由唯一的 `routeInstanceId`，同 key 双持有不可达，本轮不移植。
- `examples/types` 的 tsc 错误与上游同形同量（267 → 266，仅少一条与本批排除项相关）：其中 `tool-types.d.ts` 从 `./results` 导入未导出符号属上游继承缺陷，本批未引入也未加重。
- logo / `api_version` / `requires` 只做数据管线、UI 无消费方：个人版「包图标」属独立可选批次；`manifest.logo` 的严格校验与上游一致，不额外放宽。
- 插件拖动排序回调在主线程调用 `updateToolPkgPluginOrder`：上游既有写法，且移到 IO 会让运行态变化通知在非主线程触达全部桥接与桌面小组件，本轮不做未经验证的线程语义变化。
- `ChatArea` 长按菜单构建无异常兜底（点击路径已有 try/catch，仅长按构建那一刻无保护）、对话框宿主挂在虚拟化列表项内且 `toolPkgDialogRequest` 非 `rememberSaveable`、`FunctionModelType` 声明比运行时窄（运行时 `parseChatCallFunctionType` 按枚举名全量匹配，插件可传 `PERMISSION_REVIEWER` / `PERMISSION_RISK_SCORER`）：均与上游 v1.12.2 一致，本轮保持。
- Compose DSL 生成器脚本本轮**已随个人版 EchoGuard 改动同步更新**（相对基点 +30/−20，见 `git diff --stat 4df70cea0 -- tools/compose_dsl/generate_compose_dsl_artifacts.py`），脚本与产物一致；两者只剩一处等价写法差异（脚本是 `if (reconcile(...) != EXTERNAL_CHANGE) return@LaunchedEffect` 早返回，产物是等价的 `if (reconcile(...) == EXTERNAL_CHANGE) { ... }` 包块），将来在具备 Compose 源码 jar 的机器上重跑生成器会产生一次纯格式 diff。上游 v1.12.2 自己的脚本仍引用旧符号 `lastAppliedExternalValue`（这才是上游脚本与上游产物之间的差异），本工作树不存在该问题。「重跑生成器能否逐字节复现产物」**未验证**：`gen-compose-dsl.log` 显示 `RuntimeError: missing Compose source jars: material3, foundation-layout, foundation`。
- `ToolPkgLoadOrderResolver` / `validateToolPkgRelationships` 未补单测：构造 `ToolPkgContainerRuntime` 夹具需 20+ 个必填字段，代价与收益不成比例。

- 修复后证据：`:app:compileDebugKotlin` + `:app:compileDebugUnitTestKotlin` 成功（`build-upstream-fixes.log`、`build-upstream-fixes2.log`）；聚焦单测 33 个类、197 项、0 失败（`build-upstream-fixes-tests.log`，测试结果 XML 亦被审计代理独立复核）。
- 同批次只读复审结论：内核侧 `AUDIT: ISSUES` → 复核后「P1 已验证有效，无阻断性问题」，建议的两处收紧（超时判定、转向路径提示）已按建议落实；UI 侧复审为 `AUDIT: CLEAN`，确认 P0/P1 与个人版基点 `4df70cea0` 逐行等价、`AppContent` 的 keep-alive 门禁使 `isCurrentScreen` 语义真实生效、首次挂载不会误发 `onResume`。
- 新增源文件已用 `git add -N` 标记为 intent-to-add，使 `git diff <base>` 能看见本批新增文件。相对基点 `4df70cea0` 的规模**按提交号固定**，便于逐字复核（`git diff --shortstat 4df70cea0 <commit>`）：`d2abbea9b`（第六轮功能提交）= 143 个文件 / `+14,637` / `−1,965`；`193d48a43`（第八轮代码修复）= 144 个文件 / `+14,673` / `−1,965`（多出的文件是 `chat-parser/.../ChatUtils.kt`）；`8da7b225c`（含台账提交）= 144 个文件 / `+14,684` / `−1,965`。插入行数会随台账自身追加而变大（每加一行台账 +1；在已插入的块里改写一行则数字不变，`193d48a43` 与 `15a668416` 同为 14,673 就是这个原因），文件数与删除数不受台账影响。构建日志与测试结果日志保持未跟踪（`examples/types` 的 tsc 检查、`examples/apktool` 重建脚本、`temp/` 已被忽略）。
- 提交策略：按路径显式 `git add -u`（只纳入已跟踪文件与已 `-N` 的新文件），不纳入任何 `build-*.log`、`dts-check*.log`、`gen-compose-dsl.log` 与 `app/objectbox-jni-windows-x64.dll`；`app/libs/.keep` 与 `app/src/test/java/.../MediaLinkParserFileTest.kt` 在 `git status` 里显示为改动，但 `git diff` 内容为空（仅 CRLF 归一化），提交后不产生差异。
- 本轮为静态改动 + 编译 + 本地单测，未真机验证。

#### 最终验证（2026-09-19，静态 + 编译 + 本地单测）

- 编译：`:app:compileDebugKotlin` 与 `:app:compileDebugUnitTestKotlin` 成功（`build-upstream-fixes.log`、`build-upstream-fixes2.log`）。
- 聚焦单测（ToolPkg 批次收口）：33 个类 / 197 项 / 0 失败（`build-upstream-fixes-tests.log`）。
- **整套单测**：`:app:testDebugUnitTest` BUILD SUCCESSFUL，1m36s（`build-upstream-fulltests.log`）；`app/build/test-results/testDebugUnitTest/*.xml` 汇总 **313 个类 / 2392 项 / 0 失败 / 0 错误 / 0 跳过**。这是当前最强的一次本地验证，覆盖了此前只跑聚焦子集的缺口。
- 修复第五轮复审的 P1 之后的收口重跑：`:app:compileDebugKotlin` + `:app:compileDebugUnitTestKotlin` + `:app:testDebugUnitTest` 全部 BUILD SUCCESSFUL，2m1s（`build-upstream-final.log`），结果仍是 **313 个类 / 2392 项 / 0 失败 / 0 错误 / 0 跳过**。
- APK 逆向 helper 打包：`runtimeFatJar` + D8 成功（`build-apktool-dex.log`），产物 dex 已核对（见「第四轮」小节）。
- `examples/types` 的独立 tsc 检查：本工作树 266 条 vs 上游 v1.12.2 267 条错误，无新增（`dts-check.log` / `dts-check-upstream.log`）。
- 未运行：真机 / adb / 仪器测试（用户明确禁止）；其它模块（`chat-parser` / `quickjs` / `terminal`）的单测；`clone`/`nightly`/`release` 变体的编译。本轮改动未触及这些模块的源码。
- 未提交、未推送、未打标签、未发布。

#### 第四轮批次与审计（工具历史 / 记忆 / 摘要 / APK 逆向，2026-09-19）

**工具历史配对与跨 provider 工具调用**：`StructuredToolCallBridge` 由「按位置配对」改为上游最终的 `OpenToolCall` / `toolCallName` / `consumeMatchingToolCalls` 语义（按名/按 ID 配对、代理解包、未匹配结果诊断），并把 Claude/Deepseek/Kimi/Gemini 里原来的「User cancelled」占位**替换**为统一的 `unmatchedToolResultContent(...)`；保留个人 `callId`、`invocationIndex` 与协议体系。改动文件：`StructuredToolCallBridge.kt`、`OpenAIProvider.kt`、`DeepseekProvider.kt`、`KimiProvider.kt`、`ClaudeProvider.kt`、`GeminiProvider.kt`、`MediaLinkParser.kt`、`OpenAIResponsesProvider.kt`。
- 独立审计 `audit_provider_history`：**CLEAN**。复核确认「自闭合标签吞掉后续内容」已修、无引号 ID 不再包含 `/`；新增测试覆盖正文保留、后续图片与转义属性重排；上一轮 Responses 工具图片修复结论保持 CLEAN。
- 未匹配工具结果的占位文案：基点里的「User cancelled」字面量（`ClaudeProvider`／`DeepseekProvider`／`KimiProvider`／`GeminiProvider`）已被 `StructuredToolCallBridge.unmatchedToolResultContent` 取代，这是**替换**而不是「补齐占位」。本工作树返回 `"工具结果缺失：$toolLabel $detail。"`，上游 v1.12.2 同一行（上游文件 L120）多一句 `这不是用户取消。`。**未采用那句是刻意的**：上游自己的两个断言与那句互斥——`StructuredToolCallBridgeHistoryTest`（上游 L46／本地 L55）与 `OpenAiToolCallHistoryTest`（上游 L213／本地 L218）都断言 `assertFalse(placeholder.contains("用户取消"))`，而上游实现返回的字符串包含「用户取消」四个字，因此上游 v1.12.2 的该组断言与其实现自相矛盾。本工作树保留断言所要求的字面（占位文本不复述「用户取消」），并把上游同测试里要求带末句的两条 `assertEquals`（上游 L227-228）改为不带末句，使代码与断言一致、整套单测全绿。若日后希望模型看到该澄清，必须同时把上述两条 `assertFalse` 改成断言「明确说明不是取消」，不能只改实现。`StructuredToolCallBridge.kt:326` 是该文件内的唯一调用点，全仓共 6 处调用（该文件 + `ClaudeProvider`、`DeepseekProvider`、`GeminiProvider`、`KimiProvider`、`OpenAIProvider`），工作树内已无任何位置残留旧文案。

**记忆按日期/窗口重建**：取入 `ChatMemoryRebuildManager` / `ChatMemoryRebuildTimeScope` / `ChatMemoryWindowPlanner`（含两个上游测试）与 `MemoryLibrary.saveMemoryWindowNow`、`ChatHistorySettingsScreen` 入口；保留个人数组记忆协议（不引入上游 object 协议与 profile 文档分支），窗口保存严格传播失败、取消异常直接重抛、逐聊天加载已 hydration 的分块正文，不清空既有记忆。
- 独立审计 `audit_memory_rebuild`：**CLEAN**。确认扫描与执行都改用 `loadChatMessagesOrThrow`（读取异常 → FAILED，取消 → CANCELLED），严格分析只承认已知操作与数组类型，`{}` 与空白对象仍按跳过处理。

**摘要分区**：取入 `SummarySectionConfig` / `SummarySectionOverride` / `ConversationSummaryConfig`、`FunctionalPrompts.buildSummarySystemPrompt` 及配套函数、三个摘要入口（`AIMessageManager` / `EnhancedAIService` / `MessageCoordinationDelegate`、个人版 `ChatViewModel` 手动摘要入口）与 `ContextSummarySettingsScreen`；协作历史压缩的 checkpoint 规则单独包成 `ConversationSummaryConfig(globalRules=...)`，不套用主聊天的分区/回顾配置。
- 独立审计 `audit_summary_sections`：**CLEAN**。确认语言判断与摘要服务一致，分区规则与全局规则各自只更新自己的字段，其它设置在 DataStore 事务内保留。

**APK 逆向工具 1.1.0**（`examples/apktool`，11 个文件 +2145/−105）：整套取自 v1.12.2，包括 `manifest.json`（1.0.3 → 1.1.0，补 `api_version: 1.0.0`）、`dist`/`src`（`apk_reverse` 全量更新）、三个 `*-runtime-android.jar`、`runtime_helper` 的 `IsolatedJadxMain`/`IsolatedJadxRunner`（新增）与 `ApkReverseHelperFacade`/`JadxBridgeSupport`/`ReflectionSupport`（更新）；`runtime_patch_src` 与上游一致，未改。
- 与上游 v1.12.2 不同的文件共 5 个，其中 4 个是有意改动、1 个是本地重建产物（`git diff --stat v1.12.2 -- examples/apktool` 可复核）：① `IsolatedJadxRunner.java` 把子进程输出排空移到后台守护线程，主线程恢复限时 `waitFor(90min)` 并在 `finally` 强制清理（上游版本先阻塞读到 EOF，超时分支实际不可达）；② `manifest.json`、③ `src/packages/apk_reverse.ts`、④ `dist/packages/apk_reverse.js` 删除「可从断点续跑」宣称——`startIndex` 第 6 参数在上游从未被传入，选择删宣称而不是接通续跑；⑤ `apk-reverse-helper-runtime-android.jar` 为本机用源码重新 D8 打包的产物（含①的修复），非上游 blob。
- 打包方式：脚本 `build_runtime_android_resources.ps1` 硬编码本机不存在的 Zulu 路径且会重建全部三个 runtime，因此只抽出其中的 `Build-HelperRuntime` 单步执行（本工作树 `temp/` 下驱动脚本，`temp` 已被 `.gitignore` 忽略），R8 9.0.32 的 D8 + `--min-api 26` + `android-36/android.jar`。日志：`build-apktool-dex.log`（`runtimeFatJar` 成功）。
- 打包产物验证：`apk-reverse-helper-runtime-android.jar` 3186262 字节，`classes.dex` 含全部 10 个 helper 类、含修复标记字符串 `isolated-jadx-output`；非 class 资源集合与改动前逐条一致（未丢 bouncycastle 属性文件 / LICENSE / META-INF）。
- 独立审计 `audit_apktool`：首轮 **2 个 P2（均来自上游正式 tag）** —— 隔离进程超时不可达、断点续跑未接通。两条按上述方式修复后**复审为 `AUDIT: CLEAN`**：确认超时等待已与输出读取分离、异常路径会终止子进程并关闭线程池、正常路径 payload 仍可见；三处续跑宣称已删净、全目录无残留；`src` 转译结果与 `dist` 一致且 `node --check` 通过；新 dex 含 `isolated-jadx-output`、executor/Future/清理方法；非 class 资源仍为 16 项无增删（仅 Manifest 的 `Created-By` 由 Azul 变为 Eclipse Adoptium）。
- 未做：在真机/设备上实际运行隔离 JADX；R8 shrink 后的运行期行为未实测；续跑能力未实现（只删宣称）。

#### 第五轮：整批整体独立审计（2026-09-19）

对「基点 → 当前工作树」的**整批 143 个文件**做了一次跨批次只读审计（独立代理，`command-code/deepseek-deepseek-v4.1-flash` / high，`fork_turns=none`），目标是找批次级审计漏掉的回归与不一致。首轮结论：**`AUDIT: ISSUES`，无 P0/P1，6 个 P2 + 6 处台账偏差**；复审追加 **1 个 P1（已修复）**。处置如下：

- P2「未匹配工具结果占位少一句」：**记录不修改**，理由与上游自相矛盾的证据见「第四轮」小节；台账原先把它写成「补齐占位」也已更正。
- P2「`SpeechServicesPreferences` 的注释称旧语音字段仍是运行时投影，与个人版设计矛盾」：**记录不修改**。该文件与上游逐字节一致；个人版以语音档案为唯一运行时真源，旧 store 只在首次启动迁移时被读取、之后不再写回。可接受后果：降级回 `1.12.1-ry.8`（或任何仍读旧 store 的构建）会看到升级那一刻的冻结快照，而不是最后一次配置。若日后要消除误导，改该文件 L25-30 与 L196 的注释即可，不需要恢复投影。
- P2「`ToolPkgChatRuntimeHookBridge` / `ToolPkgChatMessageMenuItemBridge` 没有执行预算」：**记录不修改**（这两处是本批新取入的路径，上游 v1.12.2 同样没有预算）；已更正台账原先把它们说成「本批之前就存在」的错误，并记下收口做法。
- P2「`FunctionalPrompts.summaryPromptTemplate()` 用 `check()` 硬失败」：**记录不修改**（上游实现）。触发条件是摘要提示词模板不再满足分区标题/分隔线约定；审计代理已核对当前 zh（4 个标题 + 28 个 `=`）与 en（39 个 `=`）都满足，因此当前没有实际故障。个人版是主动改 prompt 的仓库，若日后要加固，做法是不匹配时回退 legacy 模板并记日志。
- P2「工作树有 22 个未跟踪且未被忽略的文件（`build-*.log`、`gen-compose-dsl.log`、`app/objectbox-jni-windows-x64.dll`）」：**记录不修改**。这些是验证证据与被复制进来的本机依赖，不属于本批源码；提交时必须按路径显式 `git add`，不要用 `git add -A`。（该计数只对当时成立；后续轮次仍在产生 `build-*.log` 一类文件，验证期间一度为 30，本文档不跟踪这个会持续变化的数字。）
- 台账偏差全部已更正：① 差异统计的插入行数少算；② 生成器脚本被误写成「未改动」；③ 把「替换 User cancelled」写成「补齐占位」；④ 无预算 hook 路径的来源写错，且漏写这些路径（当时描述为 30 分钟上限，实为无上限）；⑤ apktool 与上游的差异文件数写成 3（实际 5）；⑥ 新增测试文件清单不全、分类写错（现按「3 个个人新建 / 10 个上游逐字节一致 / 4 个按个人版适配」重写）；⑦ 「上游唯一调用点」措辞不严谨（应为该文件内唯一，全仓共 6 处）。
- 审计同时反向核验通过的关键点：版本 `100204` / `1.12.2-ry.1` 与 DEV/clone 发布身份保留；`values`（8130 key）与 `values-en`（8130 key）完全成对、零单侧；排除项（`message_persisted`、`chatMessageHooks`、`ToolPkgRuntimeMonitor`、`onHookTimeout`、`marketOrigin`、`ModelConfigConnectionTestOutcome`）全仓零命中，`activeLeases` 只作为个人版 `MultiServiceManager` 的多服务租约字段存在（该文件本批未改，与上游 `ToolPkgManager` 的引擎租约无关；第七轮已据此更正措辞）；`ToolPkgComposeDslGeneratedRenderers.kt` 与 `.tpl` 与上游逐字节一致；个人版生命周期恢复、群聊首消息复用、`ToolPermissionSystem` 并发容器、F07 桌宠入口、`MainActivity` 的 ROOM_RESTORE 入口均在位。

**复审（同一代理）追加 1 个 P1，已修复。** `PackageManagerToolPkgFacade` 把「没有预算」折算成了 `timeoutSec = null`，而 `JsEngine` 对 null 的语义是**永不超时**（`JsEngine.kt:782`/`865`/`912`，JS 侧 `JsExecutionScriptBuilder` 在 `hasTimeout == false` 时连 pre-timeout 定时器都不建）。基点在同一 Facade 上根本不传该参数，因此走引擎默认 `JsTimeoutConfig.MAIN_TIMEOUT_SECONDS = 1800`；上游 v1.12.2 用 `safeTimeoutMillis ?: timeoutSec` 同样回落到 1800。**只有本工作树把「无预算」翻译成了「无上限」**，影响所有未传预算的 hook：`ToolPkgCommonBridgePlugin`（message-processing / xml-render / input-menu，共 4 个调用点）、`ToolPkgChatViewHookBridge`、`ToolPkgToolLifecycleBridge`、`ToolboxPlugin` app lifecycle、`ToolPkgJsAiProviderService`（插件当 AI provider）、两个新桥、以及 navigation entry action 的内部转调。
- 修复：单点改 `timeoutMillis?.let { ((it + 999L) / 1000L).coerceAtLeast(1L) } ?: JsTimeoutConfig.MAIN_TIMEOUT_SECONDS`（`PackageManagerToolPkgFacade.kt:1004` 附近，新增 `JsTimeoutConfig` import），即「无预算 = 引擎默认 30 分钟上限 + 无用户提示」，与基点/上游一致；有预算的三个桥行为不变。
- 修复后证据：`:app:compileDebugKotlin` + `:app:compileDebugUnitTestKotlin` + `:app:testDebugUnitTest` BUILD SUCCESSFUL，2m1s（`build-upstream-final.log`），313 类 / 2392 项 / 0 失败。
- 同一轮复审确认成立的结论：占位文案的自洽（实现与 3 条断言一致、旧「User cancelled」文案无残留）、旧语音 store 仅迁移期读取的描述准确、apktool 的 5 个差异文件与 dex 内容属实、台账数字与实测一致（生成器脚本 30/20、`examples/apktool` 11 文件 `+2145/−105`；总差异的文件数 143 与删除行 1,965 也一致，插入行数见「实施进度」里的自指说明）。此处原写「删除行 1,935」，第七轮按实测更正。
- 权威边界：该审计为只读代码审计，未自行构建或跑测试（含本轮 P1 修复后的重跑）。真机、仪器测试、其它模块与 `clone`/`nightly`/`release` 变体编译仍未验证。

#### 第六轮：推送前 Lint 收口（2026-09-19）

`Operit/AGENTS.md` 要求「推送前对最终版本运行一次完整 `:app:lintDebug`」。首次运行 **BUILD FAILED**（12m47s，`build-upstream-lint.log`）：

`Lint found 14 errors, 594 warnings, 14 hints (and 1124 errors, 4469 warnings and 167 hints filtered by baseline lint-baseline.xml)`

基点 `4df70cea0` 的 CI 日志是 `Lint found 567 warnings, 11 hints (and 1124 errors, 4471 warnings and 167 hints filtered by baseline)`，即基点 0 个 Lint 错误。这 14 条是本批相对基点新增的，**不能**通过重新生成 baseline 或关闭检查来消除。

- 14 条明细：`ToolPkgComposeDslScreen.kt:1625/1628`（`LocalContextConfigurationRead`）、`ChatArea.kt:1252`（`LocalContextGetResourceValueCall`）、`ChatHistorySettingsScreen.kt` 的 2436/2437/2443/2455/2464/2470/2523/2537/2539/2568/2577 共 11 条（`LocalContextGetResourceValueCall`，全部在 `UnboundWorkspaceCard` 内）。
- 触发机制（已用字节码核验，避免误判为「可用旧 baseline 忽略」）：AGP `lint-api 31.13.2` 的 `LintBaseline.findAndMark` 按 **message + issueId + 文件路径** 匹配，**完全不比较行号**；每条 baseline 记录只能抑制一个实例（匹配后从 multimap 移除）。因此同一文件里这类读取的**条数**若超过 baseline 里同 message 的条数，多出来的那几条就会被报成新错误——即使这些代码与基点逐字节相同（`ChatArea` / `UnboundWorkspaceCard` 的命中行正是基点已有代码，而本批在这些文件里新增了同类的 `LocalContext` 资源读取）。
- 修复（改代码，不动 `app/lint-baseline.xml`）：① `ChatArea.kt` 的 `messageTimeSummary` 由 `remember { context.getString(...) }` 改为 `stringResource(R.string.chat_message_timestamp_compact, formatCompactTimestamp(...))`；② `ChatHistorySettingsScreen.kt` 的 `UnboundWorkspaceCard` 去掉 `val context = LocalContext.current`，改为在函数顶部 hoist 8 个 `stringResource` 文案（带参数的 3 处直接在调用点 `stringResource(id, args)`），11 个使用点全部替换；③ `ToolPkgComposeDslScreen.kt` 里两份逐字相同的 `currentLanguage`（含 `Build.VERSION_CODES.N` 分支读 `context.resources.configuration`）改为 `LocalConfiguration.current.locales[0]`，被点名的对话框宿主副本与同一文件里与之完全相同的主屏副本**都改**（后者原本只靠 baseline 抑制；`minSdk = 26`，`Configuration.locales` 无新 API 风险，与 `TokenActivitySection.kt:143` 现有写法一致）。
- 修复后证据：`:app:compileDebugKotlin` + `:app:compileDebugUnitTestKotlin` + `:app:testDebugUnitTest` + `:app:lintDebug` 合并一次运行 **BUILD SUCCESSFUL**，7m24s（`build-upstream-verify2.log`）；`Lint found 593 warnings, 14 hints (and 1122 errors, 4468 warnings and 167 hints filtered by baseline lint-baseline.xml)`，**0 个 Lint 错误**（warnings/hints 不阻塞构建）；单测结果 XML 仍是 **313 类 / 2392 项 / 0 失败 / 0 错误 / 0 跳过**。filtered 计数由 1124 降到 1122，即被修复的两条 `LocalContextConfigurationRead` 记录不再被消费，与上述修复一致。
- 边界：本轮仍是静态改动 + 编译 + 本地单测 + Lint，未真机验证（用户明确禁止）；这 14 条的修复只由主代理完成，尚未经过独立审计，PR 审计会覆盖这三处新增差异。

#### 提交与 PR（2026-09-19）

- 提交：`d2abbea9b feat: adapt upstream v1.12.2 release changes`，父提交 `4df70cea0`；分支 `codex/upstream-v1.12.2-sync` 已推送到 `origin`（`CATMIAOZHI/Operit`）。
- PR：#60，base `personal/dev`，head `codex/upstream-v1.12.2-sync`，状态 OPEN、非草稿、MERGEABLE；规模随提交增长，正文里的数字一律带提交号：开 PR 时（`d2abbea9b`）143 个文件 / `+14,637` / `−1,965`，`193d48a43` 时 144 个文件 / `+14,673` / `−1,965`，`8da7b225c` 与 `67a555cab` 时 144 个文件 / `+14,684` / `−1,965`。个人版仓库正常走直接推送，这次按用户要求走 PR。
- PR 描述要点与本文一致：取入范围、有意保留的个人版行为、在取入代码之上的修复、验证证据与未验证边界。PR 正文由 `temp/pr-body.md` 生成（`temp/` 已被忽略，不入库）。
- 未合并：按用户流程等待独立审计结论，再决定后续处理；本文档「第七轮」起的各轮复审（最新一轮见节号最大的小节）之后仍未合并，等用户决定。

#### 第七轮：PR #60 整批独立审计与处置（2026-09-19）

独立只读审计代理（`command-code/deepseek-deepseek-v4.1-flash` / high，`fork_turns=none`，未参与实现）对 PR #60 的最终差异做了整批审计，复审对象为 `f9a1c93b0`（当时的分支 HEAD）。首轮结论 **`AUDIT: ISSUES`，0 个 P0、0 个 P1、4 个 P2**。处置如下：

- **P2（真问题，已修）**：`StructuredToolCallBridge.kt:340` 本批新增的历史清洗只调用 `ChatUtils.stripOpenAiResponsesReasoningMeta`，上游同一行是 `ChatUtils.stripOpenAiResponsesProtocolMarkup`（reasoning meta + 去 `<search>` 块 + `trim`）。修复：在 `chat-parser` 的 `ChatUtils` 新增 `stripOpenAiResponsesProtocolMarkup`，调用点改用它。与上游仍差一层——上游还会去掉 `openai:responses_output_item` 的 meta，而个人版 `ChatMarkupRegex` 没有该 provider 常量（个人版从未处理 output-item meta），该差异写进了新函数的 KDoc。相对基点是补齐（基点这一行完全没有清洗），不是回归。
- **P2（台账措辞）**：第 116 行原称排除项（含 `activeLeases`）「全仓零命中」；实测 `activeLeases` 在个人版 `MultiServiceManager.kt:61` 有真实使用（与上游 `ToolPkgManager` 引擎租约无关的个人版字段，该文件本批未改）。已改写。
- **P2（台账数字）**：第 121 行原写「删除行 1,935」，实测 1,965（与第 72 行自相矛盾）。已更正。
- **P2（台账措辞）**：第 36 行原称 tsc「错误集合逐条一致」；实测条数为 266 vs 267，按（文件、行号、规则）比对一致，但 `java-bridge.d.ts` 有 9 条报文文本不同（该文件本身是个人版内容，非本批引入）。已改写。
- 审计独立核实成立（没有复述台账）：三处 Lint 修复行为等价（资源 id、参数个数与顺序、`?: "en"` 兜底语义都不变；`minSdk = 26` 使被删掉的 `SDK_INT >= N` 分支本来就是死代码）；单测 313 类 / 2392 项 / 0 失败，且每个含 `@Test` 的类都有对应结果 XML；Lint 0 errors 且 `app/lint-baseline.xml` 在本批两个提交上 `git diff` 均为空；baseline 匹配机制经 `javap` 字节码核实；远端分支 HEAD 与 PR 元数据无漂移；冲突标记 0 处、zh/en 字符串 8130/8130 成对；`call_chat_model` 已注册，记忆重建 / 摘要分区 / 群聊首消息复用均在位。
- 审计未能核实（不运行构建）：HEAD 可编译只有间接证据；真机、仪器测试、`clone`/`nightly`/`release` 变体、Compose DSL 生成器重跑、`chat-parser`/`quickjs`/`terminal` 模块单测均未覆盖。
- 修复后验证（第七轮）：`:app:compileDebugKotlin` + `:app:compileDebugUnitTestKotlin` + `:app:testDebugUnitTest` + `:app:lintDebug` 合并一次运行 **BUILD SUCCESSFUL**，7m57s（`build-upstream-verify3.log`）；单测结果 XML 仍是 **313 类 / 2392 项 / 0 失败 / 0 错误 / 0 跳过**。该次 `:app:lintAnalyzeDebug` 是重新执行（分析产物 21:22:22 刷新），但 `:app:lintReportDebug` 因问题集合与第六轮完全相同而判定 UP-TO-DATE，没有重新打印汇总行；为拿到最终版本的当场证据，随后单独强制重跑该报告任务：`gradlew :app:lintReportDebug --rerun` → BUILD SUCCESSFUL，26s（`build-upstream-lint3.log`），`Lint found 593 warnings, 14 hints (and 1122 errors, 4468 warnings and 167 hints filtered by baseline lint-baseline.xml)`，即最终版本仍是 **0 个 Lint 错误**。
- 随后把修复后的差异（HEAD `15a668416`）发回**同一**审计代理复审（只读）。复审结论仍为 `AUDIT: ISSUES`：三处台账改写属实、上轮 P2-1 的修复本身正确，但发现同类问题还有一处未修（见第八轮），并指出台账数字与 PR 描述未随本轮更新。

#### 第八轮：PR #60 复审（第二轮）与处置（2026-09-19）

同一独立代理对 `f9a1c93b0..15a668416` 的增量与全部 `stripOpenAiResponses*` 调用点做了第二轮只读复审。结论 **`AUDIT: ISSUES`，0 个 P0、0 个 P1、4 个 P2**，处置如下：

- **P2（真问题，已修）**：`OpenAIProvider.kt:1004` 的 `inputContentText` 是本批**新引入**的函数（基点是 `buildContentField(context, text)`），其 else 分支（非 Responses API 路径）同样只做 `stripOpenAiResponsesReasoningMeta`，上游是 `stripOpenAiResponsesProtocolMarkup`。影响面比第七轮那处更宽：`inputContentText` 是 `buildContentField` 的唯一入口，后者被该文件内部 10 处调用点以及 `KimiProvider` 等 OpenAI 兼容 provider 复用，图片/媒体链接扫描也会经过它。修复：该行改用 `ChatUtils.stripOpenAiResponsesProtocolMarkup(text)`（一处一行）。
- **P2（台账数字未随第七轮更新）**：第 72、141 行原先还写着 143 个文件 / `+14,637`，而 `chat-parser/.../ChatUtils.kt` 已进入本批，实测文件数已到 144。已改成**按提交号固定**的写法并逐字核对：`d2abbea9b` = 143 / `+14,637` / `−1,965`；`193d48a43` = 144 / `+14,673` / `−1,965`（这一处插入数与其父提交相同，因为修的是已插入块里的一行）；`8da7b225c` = 144 / `+14,684` / `−1,965`。同时注明插入行数会随台账自身追加而变大、文件数与删除数不受台账影响。
- **P2（台账措辞：预写结论）**：第七轮末句原先把复审结果直接写成「直到结论 CLEAN」。已改为如实记录复审仍为 ISSUES 并指向本节。
- **P2（PR 描述落后于台账）**：PR 正文 Validation 段的 tsc 措辞与已更正的第 36 行不一致。已同步 PR 正文（并把本轮 stripper 修复补进「Fixes carried on top of the upstream code」）。
- 复审确认成立、且明确**不属于本批问题**的个人版长期分歧（基点 `4df70cea0` 已如此，本批 diff 在这些文件里没有任何相关增删行），本轮有意不改：`ConversationService.kt:165` 用 `stripOpenAiResponsesReasoningMetaTurns`（上游对应位置 131 行是 `stripOpenAiResponsesProtocolMarkupTurns`），以及 `ClaudeProvider` / `GeminiProvider` / `DeepseekProvider` / `LlamaProvider` / `MNNProvider` 的历史构建仍走各自的 `removeThinkingContent` 或不清洗。上游在这 5 个文件里的 `ProtocolMarkup` 调用点实测为 **10 处**（Claude 726/857、Deepseek 1041/1055、Gemini 266/652/668/803、Llama 383、MNN 521；可用 `git grep -n stripOpenAiResponsesProtocolMarkup dbf71916f -- <这 5 个文件>` 复核）。改动它们属于个人版长期分歧的重新定位，超出「吸收上游 v1.12.2」的范围。
- 修复后验证（第八轮）：`:app:compileDebugKotlin` + `:app:compileDebugUnitTestKotlin` + `:app:testDebugUnitTest` + `:app:lintDebug` + `:app:lintReportDebug --rerun` 合并一次运行 **BUILD SUCCESSFUL**，7m45s（`build-upstream-verify4.log`）；其中 `:app:compileDebugKotlin`、`:app:lintAnalyzeDebug`、`:app:testDebugUnitTest`、`:app:lintReportDebug`（`--rerun` 生效，无 UP-TO-DATE 后缀）与 `:app:lintDebug` 都是执行态，`:app:compileDebugUnitTestKotlin` 是 UP-TO-DATE（该行改动未变更其输入）。单测结果 XML 仍是 **313 类 / 2392 项 / 0 失败 / 0 错误 / 0 跳过**；该次当场打印 `Lint found 593 warnings, 14 hints (and 1122 errors, 4468 warnings and 167 hints filtered by baseline lint-baseline.xml)`，即最终版本 **0 个 Lint 错误**，`lint-results-debug.txt` 里 `Error:` 计数为 0。

#### 第九轮：PR #60 复审（第三轮）与处置（2026-09-19）

同一独立代理对 `d12a0b3cc` 的增量做了第三轮只读复审。结论 **`AUDIT: ISSUES`，0 个 P0、0 个 P1，2 个文档级 P2**（第八轮那 4 个 P2 已被确认全部闭环）：

- **P2（台账数字，源头是上一轮报告里的口误）**：第八轮小节原写「上游在这 5 个文件里有 11 个 `ProtocolMarkup` 调用点」，实测 **10 处**。已改为 10 处并给出可复核清单（Claude 726/857、Deepseek 1041/1055、Gemini 266/652/668/803、Llama 383、MNN 521；`git grep -n stripOpenAiResponsesProtocolMarkup dbf71916f -- <这 5 个文件>`）。
- **P2（台账措辞自相矛盾）**：第 143 行原写「第七轮复审通过后仍未合并」，与记录在案的 ISSUES 结论矛盾。已改为「第七、八、九轮复审后仍未合并，等用户决定」。
- 另按复审的精度提示，在第八轮验证条目里写明了该次运行中每个任务的执行态（`:app:compileDebugUnitTestKotlin` 是 UP-TO-DATE 及其原因）。
- 本轮处置提交 `67a555cab` **只改台账**（`git diff --name-only 193d48a43 HEAD` 只有该文件；`git diff --shortstat 4df70cea0 67a555cab` 仍是 144 文件 / `+14,684` / `−1,965`），源码未变，因此没有重跑构建；可编译 / 单测 / Lint 结论仍以第八轮那次合并运行（`build-upstream-verify4.log`）为准，其源码与当时 HEAD（`67a555cab`）完全一致。**更正（第十六轮）**：该等式只对到 `c131bd08c`；自 `babf69625` 起本批源码的构建证据是 `build-upstream-verify6.log`。本文档一律按提交号引用规模数字，不回填会持续变化的值：给台账**加行**会让 `…4df70cea0 HEAD` 的插入数变大（文件数与删除数不变），而只改写既有插入块里的行则不变。

#### 第十轮：PR #60 复审（第四轮）与处置（2026-09-19）

同一独立代理对 `67a555cab` 的状态做了第四轮只读复审。结论 **`AUDIT: ISSUES`，0 个 P0、0 个 P1，1 个记账项**（第八轮那 4 个 P2 已确认闭环）：

- **记账项（状态行过期 + 第三轮复审未入册）**：当时的第 5 行还写着「等待第三轮复审结论」，而第三轮的结论此时已经收到并已被 `67a555cab` 处置；同时文档里没有任何小节记录第三轮的结论与处置（小节从第八轮直接跳到「后续实现路线」）。
- 处置提交 `7aec4973f`：新增第九轮小节、把状态行改成不复述会过期结论的写法、并按该轮提示把「第九轮」小节移到「第八轮」之后（纯位置调整，正文未改）。
- 该轮仍无源码改动，构建证据沿用第八轮那次合并运行。

#### 第十一轮：PR #60 复审（第五轮）与处置（2026-09-19）

同一独立代理对 `7aec4973f` 的增量做了第五轮只读复审。结论 **`AUDIT: ISSUES`，0 个 P0、0 个 P1，2 个文档级 P2**（第九轮那 2 个文档级 P2 已被确认全部闭环）：

- **P2（用 `HEAD` 标签引用的规模数字不可复现）**：本文件原有一句「`git diff --shortstat 4df70cea0 HEAD` 仍是 144 文件 / `+14,684` / `−1,965`」，但那句话所在的提交本身又给台账加了行，按字面在 HEAD 上执行会得到更大的插入数。已把标签改为 `67a555cab`（可逐字复核），并写明本文档的取值政策。
- **同类次要点（无锚点的 `+14,684`）**：PR 小节的规模描述里最后半句没有提交号。已改为把每个数字都带上提交号（`d2abbea9b` / `193d48a43` / `8da7b225c` 与 `67a555cab`）。
- 复审确认：第 72 行的「按提交号固定」那组数字逐字复核通过；该轮仍无源码改动，构建证据沿用第八轮。

#### 第十二轮：PR #60 复审（第六轮）——结论 CLEAN（2026-09-19）

同一独立代理对 `bcd374afc` 的增量做了第六轮只读复审。第十一轮的 2 处记账项已确认闭环，本轮**没有新问题**（该轮给出的 `AUDIT: CLEAN` 只覆盖当时那个对象；后续轮次只针对台账措辞，未再触及代码）：

- **结论：`AUDIT: CLEAN`，0 个 P0 / 0 个 P1 / 0 个 P2。**
- 复审建议并已被本文档采纳：不要再补写当前 HEAD 的插入数——代表本批**代码侧**规模的锚点是最后一个代码提交 `193d48a43`（144 个文件 / `+14,673` / `−1,965`），第 72 行已钉住。
- **CLEAN 覆盖**：基点 `4df70cea0` → HEAD 的 144 文件代码差异（合并缝重点项：ToolPkg hook 预算及其消费方、Compose DSL 生命周期双门禁、provider 工具历史配对、记忆重建、摘要分区、apktool 隔离 JADX runner）、两处 stripper 修复与其调用点唯一性、排除能力清单零命中、台账与 PR 描述的事实一致性、以及验证证据（编译 / 单测 / Lint）与远端、PR 元数据无漂移。
- **CLEAN 不覆盖**：审计代理未亲自跑 Gradle / 单测 / Lint（只读约束，仅靠日志、产物时间戳与源码 mtime 交叉印证）；未真机验证、未 adb；未覆盖 `clone` / `nightly` / `release` 变体；未覆盖 `chat-parser` / `quickjs` / `terminal` 模块单测；未重跑 Compose DSL 生成器；两个 stripper 调用点没有针对性单测（只有与上游的同形比对）。
- 第三轮复审的收尾确认（这条属于第九轮，只是因后续小节插在其后而排在下方）：全仓 `stripOpenAiResponsesProtocolMarkup` 只有定义与两个调用点（`StructuredToolCallBridge.kt:340`、`OpenAIProvider.kt:1004`），本批新增行里没有其它仍走窄函数的路径；`MemoryLibrary.kt` 新增的 `removeThinkingContent` 与上游同形（上游 667 行同为 `removeThinkingContent`）。

#### 第十三轮：PR #60 复审（第七轮）与处置（2026-09-19）

收尾确认轮：同一独立代理对 `57b349440`（把第五、六轮结论写进台账的那次纯台账提交）做了第七轮只读复审。结论 **`AUDIT: ISSUES`，0 个 P0、0 个 P1，3 个文档级 P2**。复审确认「自判 CLEAN 之后源码与验证产物零变化」成立，但指出新写的两个小节**轮次归属错位**：

- **P2（轮次与对象配错、第四轮记录缺失）**：原「第十轮」把自己写成「第四轮、对象 `67a555cab`」，而它处置的两条（`HEAD` 标签不可复现、无锚点的 `+14,684`）属于对 `7aec4973f` 的复审；真正对 `67a555cab` 的那一轮（1 个记账项）当时在文档里没有记录。已按「一节一轮、节号即轮次」重排：第七轮 = 整批首轮、第八轮 = 复审第 2 轮、第九轮 = 复审第 3 轮、**第十轮 = 复审第 4 轮（对象 `67a555cab`）**、**第十一轮 = 复审第 5 轮（对象 `7aec4973f`）**、**第十二轮 = 复审第 6 轮（对象 `bcd374afc`，CLEAN）**、本节 = 复审第 7 轮（对象 `57b349440`）。
- **P2（状态行「共五轮」少一轮，且用轮号指代结论）**：当时实际已完成六轮（整批 1 轮 + 复审 5 轮），其中第五轮结论是 ISSUES、CLEAN 出自第六轮。状态行已改为按**对象提交号**记录，不再用「第 N 轮」指代结论。
- **P2（第九轮的一条收尾条目被后插小节挤到别轮下面）**：该条目已加注「这条属于第九轮」标明归属，保持最小改动。
- 该轮仍无源码改动；本轮处置提交 `8eaa60293`（只改台账）因此也没有重跑构建，构建证据继续沿用第八轮那次合并运行（其源码与当时 HEAD 的 `app`/`chat-parser` 逐字节相同）。**更正（第十六轮）**：这句里的「与 HEAD 逐字节相同」只对到 `c131bd08c`；`babf69625` 改了 `app` 源码，此后本批源码的构建证据是 `build-upstream-verify6.log`。

#### 第十四轮：PR #60 复审（第八轮）与处置（2026-09-19）

同一独立代理对 `e75c80930` 的状态做了第八轮只读复审。结论 **`AUDIT: ISSUES`，0 个 P0、0 个 P1，2 个文档级 P2**（它同时确认第七轮那 3 条已闭环，且「节号即轮次」的重排与它实际做过的七轮对象、结论一一对应，无跳号）：

- **P2（状态行与文档不符，且第一轮缺对象）**：状态行原写「小节标题里写了该轮复审的对象提交号」，但实测标题里只有轮次，对象提交号写在每个小节的正文首句；而且第七轮（第一轮）的正文压根没写对象 `f9a1c93b0`（全文只作为 `f9a1c93b0..15a668416` 的区间下界出现过），「一轮一对象」的索引在首轮是断的。已改：状态行改成「每一轮的小节正文首句都写明该轮复审的对象提交号」，并在第七轮正文补上「复审对象为 `f9a1c93b0`（当时的分支 HEAD）」。
- **P2（「未合并」那条仍在枚举旧轮号）**：它从「第七轮复审通过后」改成「第七、八、九轮复审后」仍是枚举，随着轮次增加必然再次过期。已改为不枚举：「本文档『第七轮』起的各轮复审（最新一轮见节号最大的小节）之后仍未合并」。
- 该轮仍无源码改动；本轮处置只改台账，因此没有重跑构建，构建证据继续沿用第八轮那次合并运行。

#### 第十五轮：PR #60 复审（第九轮）——结论 CLEAN（2026-09-19）

同一独立代理对 `0a72fd4a5` 的增量做了第九轮（收尾）只读复审。第八轮那 2 个文档级 P2 已确认闭环，本轮**没有新问题**：

- **结论：`AUDIT: CLEAN`，0 个 P0 / 0 个 P1 / 0 个 P2。**
- 复审确认：状态行的新写法与文档一致（逐节抽查第七轮到第十四轮，每节正文首句都含对象提交号；标题带 CLEAN 的只有第十二轮）；第 143 行改成不枚举后不会再随轮次过期；第十四轮小节如实记录了它第八轮的结论与处置。
- 复审给出的可合并性判定：此前各轮提出的问题**全部**是台账文档措辞 / 记账类，不影响代码差异与验证结论，也不影响 PR 可合并性；合并前需要的外部决定只有「用户是否授权合并」。据此结束迭代式的台账文字打磨。
- 复审独立核实：`git diff --name-only 193d48a43 HEAD` 只有台账；`git diff 4df70cea0 bcd374afc -- app chat-parser` 与 `… 4df70cea0 HEAD -- app chat-parser` 逐字节相同（可用 `git diff` 自行复核），因此第八轮那次构建证据仍对应当前源码；单测 313 个 XML / 2,392 项 / 0 失败 / 0 错误 / 0 跳过；lint 文本报告 `Error:` 计数 0；`app/lint-baseline.xml` 在本批 `git diff` 为空；144 个改动文件冲突标记 0；PR #60 元数据（144 文件 / `+14,736` / `−1,965`、OPEN、非草稿、MERGEABLE）与远端无漂移。**更正（第十六轮）**：这两条对应关系以 `c131bd08c` 为界——`babf69625` 之后 `git diff --name-only 193d48a43 HEAD` 是两个文件（本台账 + `RoomDatabaseRestoreManager.kt`），`… 4df70cea0 HEAD -- app chat-parser` 也不再与 `bcd374afc` 逐字节相同，构建证据改以 `build-upstream-verify6.log` 为准。
- CLEAN 覆盖：第十二轮列出的范围（基点 `4df70cea0` → HEAD 的 144 文件代码差异、两处 stripper 修复与其调用点唯一性、排除能力清单零命中、验证证据与远端 / PR 元数据无漂移），加上「台账与 PR 描述的事实一致性（含状态行与第七～十四轮的轮次—对象映射）」。
- CLEAN 不覆盖：审计代理未亲自跑 Gradle / 单测 / Lint（只读约束，仅靠日志、产物时间戳与源码 mtime 交叉印证）；未真机验证、未 adb；未覆盖 `clone` / `nightly` / `release` 变体；未覆盖 `chat-parser` / `quickjs` / `terminal` 模块单测；未重跑 Compose DSL 生成器；两个 stripper 调用点没有针对性单测（只有与上游同形比对）；早期的跨轮转述（如第十轮那句「第八轮那 4 个 P2 已确认闭环」由哪一轮确认）审计代理无法回溯，因为它的上下文里只有第五轮之后的报告原文。

#### 第十六轮：CodeQL 新增高危告警（zip-slip）与修复（2026-09-19）

处置对象：`babf69625`（代码修复提交）。此前最后一个被判 CLEAN 的对象是 `0a72fd4a5`（第十五轮），该 CLEAN 结论由 `c131bd08c` 记录。

第十五轮 CLEAN 之后，PR #60 的 CodeQL 检查报失败：`java-kotlin` 分析产生 1 条新增告警，其余 6 个语言分析全部通过（失败的 check-run 为 `105908370979`）。告警 #67：规则 `java/zipslip`（Arbitrary file access during archive extraction），严重级别 high，位置 `app/src/main/java/com/ai/assistance/operit/data/backup/RoomDatabaseRestoreManager.kt:197`，注解原文 `Unsanitized archive entry, which may contain '..', is used in a file system operation.`。

根因：`extractArchive()` 属本批**新增**代码。它先用固定白名单校验 `entry.name`，再用这个名字构造写入路径 `FileOutputStream(File(workspace, entry.name))`。「归档提供的名字 → 路径 → 文件操作」这条数据流在代码里确实成立，CodeQL 不解释集合成员判断，因此照报。实际不可利用：`expected` 就是 `setOf(DB_NAME, "$DB_NAME-wal", "$DB_NAME-shm")`，含 `..` 的条目名不在集合内会被直接跳过；`workspace` 本身也已在 `workspace(context, pending)` 里校验过父目录与 `room-restore-` 前缀。结论：是代码坏味道并会卡住 PR 检查，不是真实漏洞。

修复（`babf69625`，`fix(backup): resolve restore archive entries to constant targets`）：把 `if (entry.name in expected)` 换成 `when (entry.name)` 相等分派，每个被接受的名字解析到由常量构造的目标，归档名从此只参与比较、不参与路径构造；并保留 `target.canonicalFile.parentFile == workspace.canonicalFile` 作为纵深防护，防止将来放宽接受集合时重新引入。

行为等价（由独立审计逐条核实）：接受集合不变；`check(!entry.isDirectory && seen.add(entry.name))` 的短路顺序与抛出的 `IllegalStateException` 不变；接受与跳过两条路径都仍然调用 `input.closeEntry()`；循环后的缺主库校验、SQLite 头校验、`output.fd.sync()` 与 `workspace()` 校验全部不变；新增的 canonical 检查对这三个常量恒为真，不会误拒有效归档。

验证证据 `build-upstream-verify6.log`：`:app:compileDebugKotlin :app:testDebugUnitTest :app:lintDebug :app:lintReportDebug --rerun` → `BUILD SUCCESSFUL in 7m 22s`、`EXIT=0`；单测 313 个 XML / 2,392 项 / 0 失败 / 0 错误 / 0 跳过；Lint 0 error（593 warnings + 14 hints，与上一轮一致）。编译产物中能查到修复后才出现的字符串 `Invalid database archive entry`，说明该次构建编译的确实是 `babf69625` 的源码。

证据指针更正：本文档第 176、211、228 行原写的「沿用第八轮那次合并运行（其源码与当前 HEAD 的 `app`/`chat-parser` 逐字节相同）」「`git diff --name-only 193d48a43 HEAD` 只有台账」在 `babf69625` 上已不成立——该命令现在返回两个文件（本台账 + `RoomDatabaseRestoreManager.kt`），且 `git diff 4df70cea0 bcd374afc -- app chat-parser` 与 `git diff 4df70cea0 babf69625 -- app chat-parser` 的差异流哈希分别是 `8e686ddd9eac7e7d0fbeafe726e5b3600e8b1e9a` 与 `3d522499ebdc73dab6bb8ad13eb7a31845a42325`；三处已就地加注更正，本批源码的构建证据自 `babf69625` 起改用 `build-upstream-verify6.log`。

CodeQL 复跑结果：修复推送后按 `pull_request` 事件重跑 `codeql.yml`（run `35449448150`，对象 `babf69625`），矩阵里的 7 个语言分析（`actions`、`c-cpp`、`go`、`java-kotlin`、`javascript-typescript`、`python`、`rust`）全部 `success`（含此前失败来源 `java-kotlin`，17m40s），汇总检查 `CodeQL` 转为 `pass`；告警 #67 的最近实例状态变为 `fixed`，`code-scanning/alerts?state=open&pr=60` 返回 0 条。仓库里仍为 open 的 4 条 `java/zipslip`（#45 `ui/features/chat/components/ExportDialogs.kt:907`、#46/#47/#48 `util/ArchiveUtil.kt:150/215/288`）全部挂在 `refs/heads/personal/main`，既不在本 PR 的改动文件内，也属于本批之前就存在的基线告警，本 PR 既未引入也未修复它们。

本轮独立只读审计（对象 `babf69625`，未参与实现）：结论 0 个 P0 / 0 个 P1 / **1 个 P2**。P2 内容即上面这条「证据指针更正」——台账当时仍声称第八轮构建证据对应当前源码、`git diff --name-only 193d48a43 HEAD` 只有台账，且全文没有提及本次修复与 `build-upstream-verify6.log`；本轮已按审计要求补上本节并就地更正那两行。审计同时独立复核了行为等价、污点论证（本文件内所有文件系统 sink 都只用常量或调用方路径）、全 PR 范围内没有其他「归档条目名 → 文件操作」的新增路径，以及 PR 元数据（144 文件 / `+14,760` / `−1,965`、OPEN、非草稿、base `personal/dev`）。**该轮审计未运行 Gradle / 未真机 / 未重跑 CodeQL**，因此当时它对「告警是否已消除」不作判断；上面的 CodeQL 复跑结果由本轮另行取得。

#### 第十七轮：第十六轮修复的复审与处置（2026-09-19）

复审对象：`c6563fc02`（第十六轮的台账提交，`babf69625` 之上只改 `docs/agent/upstream-reviews/v1.12.2.md`）。同一独立只读代理结论 **`AUDIT: ISSUES`，0 个 P0、0 个 P1、3 个文档级 P2**；第十六轮那条 P2 的实质内容被确认已闭环（修复、证据、两处更正都在位，轮次编号连续无跳号），下面 3 条都是本轮编辑新引入或遗漏的：

- **P2（第十五轮的对象写错，且与文档自身矛盾）**：新小节原写「此前最后一个被判 CLEAN 的对象是 `c131bd08c`（第十五轮）」，第 228 行的注记也写「以本轮对象 `c131bd08c` 为界」。但第 5 行与第 223 行都写明第十五轮的对象是 `0a72fd4a5`；`c131bd08c` 只是记录该结论的提交，从未作为审计对象。已改为「此前最后一个被判 CLEAN 的对象是 `0a72fd4a5`（第十五轮），该 CLEAN 结论由 `c131bd08c` 记录」，注记改为「以 `c131bd08c` 为界」。
- **P2（第 176 行漏改）**：第十轮那条仍写「其源码与当前 HEAD 完全一致」，是同一类会随源码变化失效的断言，但上一轮只改了第 211、228 行。已在第 176 行就地改为「与当时 HEAD（`67a555cab`）完全一致」并加「更正（第十六轮）」注记，正文的更正说明也从「第 211 行与第 228 行」改成「第 176、211、228 行」。
- **P2（语言分析个数多算一个）**：`.github/workflows/codeql.yml` 的矩阵只有 7 种语言（`actions`、`c-cpp`、`go`、`java-kotlin`、`javascript-typescript`、`python`、`rust`；文件里注明 Swift 被有意省略），run `35449448150` 也只有 7 个 `Analyze (…)` 作业。原文「其余 7 个语言分析全部通过」与「8 个语言分析全部 `success`」已分别改为「其余 6 个」和「矩阵里的 7 个语言分析（逐个列出）」，PR 正文的同一句也一并改掉。

本轮顺带处理的记账项：第 114 行那句「工作树有 22 个未跟踪且未被忽略的文件」是当时成立的记录，之后构建继续产生 `build-*.log`，验证期间已是 30；已就地注明该计数只对当时成立、本文档不跟踪它。

审计另指出（未定性为问题、但此前台账完全没提）：本 PR 上还有一个与本次修复无关的红色检查 `github-advanced-security`（工作流「GitHub Advanced Security」，标题「Code scanning AI findings on PR #60」，事件 `dynamic`）。本代理独立核实：它在 `c131bd08c`、`babf69625`、`c6563fc02` 上都是 `failure`，作业里失败的是第 19 步 `Processing Request (Linux)`，属 GitHub 托管侧的 AI 找隐患作业；`personal/dev` 的 ruleset「Protect personal/dev」只启用 `deletion` 与 `non_fast_forward` 两条规则、没有任何必填状态检查，因此这条红色检查既不阻塞合并，也不是本项目工作流产生的。

### 后续实现路线（不能把本节当已交付）

本节是初次筛选时的路线清单，逐条状态如下（以正文为准）：1–9、13 已交付；10（DeepSeek Responses 专用端点）、11（手动健康修复）、12 的性能面板/日语翻译/包图标仍未做，未做原因见对应条目与下表。

1. F01 审计闭环、隔离文件故障测试；F02/F03与视频聚焦测试；清除构建生成漂移只能针对新工作树自有生成物。
2. provider（**已交付**，原文为计划）：`StructuredToolCallBridge` 已按上游最终 `OpenToolCall`/`toolCallName`/`consumeMatchingToolCalls` 语义重写，代理解包与剩余调用已补齐；保留个人调用 ID 与协议体系。注意：基点里 Claude/Deepseek/Kimi/Gemini 的「User cancelled」字面量是被**替换**（不是被补齐），替换后的占位文案有意不含上游末句，理由见「第四轮」小节。
3. group 首消息 `567f245ad`：协调层传 prebuiltMessageContent，处理层复用内容；个人版应按原 message timestamp 去重，不盲删最后一条。
4. F07 悬浮尺寸/熔断；F08 Compose 输入 echo guard/dispatch queue（不可覆盖个人已有TextFieldValue逻辑）。
5. 语音档案：可取 VersionedPreferencesDataStore、SpeechServiceProfilesPreferences 新文件，只迁移新档案域；接工厂/设置/重置。同ID编辑必须失效缓存。
6. 摘要分区：取模型新增SummarySectionConfig/Override/ConversationSummaryConfig、prompt和UI/配置保存；接readSummaryConfig→AIMessageManager→EnhancedAIService→ConversationService，保留个人压缩算法/工具预算。
7. 记忆日期/窗口：取 ChatMemoryRebuildTimeScope/WindowPlanner及测试；Manager适配逐聊天加载。个人版已有角色卡memoryProfile绑定。MemoryLibrary加saveMemoryWindowNow；沿用memory-storage与ObjectBox UID；不清空记忆。
8. ToolPkg：先manifest/API/依赖排序，再JS运行桥和hook，再Compose/菜单。`ToolPkgApiVersion`上游版本正则不接受Ry后缀，必须适配。默认API1.0.0保留旧包。JsTools新版依赖__operitToolPkgApi初始化及call_chat_model原生接线。不要恢复个人版有意删除的market/旧hook/预算。新增runtime hook不替代collaboration v2。
9. variant事件随ToolPkg一起接：当前ChatHistoryDelegate无message_persisted；使用事务内新增variant实际快照，保留现有物化DAO。
10. DeepSeek Responses：完整最终端点adapter + provider +搜索配置+隐藏output metadata。个人ModelProtocol转换会丢provider身份，必须明确专用协议，不能URL模糊猜测。保留commentary/reasoning回放及服务端工具不执行边界。
11. 手动健康修复：只隔离副本验证个人schema33；不要复制上游AppDatabase或把任意{/[开头自定义文本判坏后删整个DataStore。
12. 性能面板需要QuickJS JNI memory/tid接线和runtime overview，采样不能被长JS永久阻塞；APK逆向examples/apktool可取最终上游整套（个人无差异）。选择剩余UI/翻译/服务入口时记录逐项结论。
13. 更新个人版本 `1.12.2-ry.1`；dev versionCode按构建手册从100203递增，不能用上游51；保留DEV/clone身份与发布链。最终相关验证、独立审计和台账闭环。
- 已遍历范围内完整非 merge 提交列表，并检查主要候选的补丁及个人版实现。下表是选项筛选，不是对 749 个变化文件的逐行审计，也不是实施完成台账。
- 上一次上游边界：`v1.12.1` → `4faa5cd2ae0b5ee2ffa94f21d6e43c5ca011f84a`。
- 本轮上界：`v1.12.2` → `dbf71916fae9750cfdc9f9a774f5a0fee56633fb`。
- 当前个人版：`personal/dev` / `origin/personal/dev` → `4df70cea029fc87d5d1a978512606897609d4bfe`。
- 旧台账仍写 PR #46 尚未合入，但 `1c706069a` 已是当前 HEAD 祖先。本地没有 `upstream-review/*` 标签；不能声称存在正式检查点。使用旧台账的上游 release_sha 作为本轮下界，不用 PR 合并提交代替。
- 已有未提交修改：`memory-storage/objectbox-models/default.json.bak`；另有未跟踪日志、工作树、DLL 和包产物。本轮未触碰它们。

恢复时：

```powershell
git rev-parse HEAD origin/personal/dev v1.12.1 v1.12.2
git status --short --untracked-files=no
git log --no-merges --reverse --format='%h %s' v1.12.1..v1.12.2
gh release view v1.12.2 --repo AAswordman/Operit --json tagName,publishedAt,assets
```

若基线或上游 tag / 资产变化，先重新比较；不能把本次静态判断当作新基线上的验证。

## 发布边界

- Release：https://github.com/AAswordman/Operit/releases/tag/v1.12.2
- 发布：2026-09-18 04:20:29 UTC（北京时间 12:20:29）。
- 非 draft、非 prerelease。
- tag 中 `versionCode=51`，`versionName=1.12.2`。
- APK：`app-release.apk`，404265417 bytes。
- GitHub digest：`sha256:a04d283464b72d5734af22bff77a025c2b56b5679a95cbe5d57b8c31b41c0cb0`。
- 元数据已查询；未下载 APK，不把服务端 digest 描述为本地验包结果。
- 范围共 353 个提交，239 个非 merge 提交；最终差异涉及 749 个文件。
- 刷新时 `upstream/main` 正好指向该 release SHA，范围外后续提交数为 0。
- 已刷新 upstream、origin；未合并任何代码。

## 第一批：建议优先采用的修复

`ADOPT` 表示适合小幅吸收，`ADAPT` 表示目标适用但必须按个人版实现适配。均未实施。

| ID | 建议 | 上游提交 | 当前代码证据及个人版影响 | 实施前/后最低验证 |
| --- | --- | --- | --- | --- |
| F01 | ADAPT：恢复失败时保住原数据库，最高优先 | `acdcdcfc9` | `ChatBackupSettingsScreen.kt:1823/1832` 仍调用 `RoomDatabaseRestoreManager`；其恢复路径先删除 db/wal/shm，再 rename/copy。替换失败会失去旧库。上游已有替换保护，但依赖其恢复 barrier，不能直接照抄；个人版另有 RawSnapshot 恢复路径，须保持边界清晰 | 用隔离数据库注入替换失败；核对 db/WAL/SHM 成组一致性、恢复门禁及重新打开；不得在真机正式数据上故障注入 |
| F02 | ADAPT：大聊天 TXT/HTML 流式导出 | `529a0b83b` / PR #892 | 当前 `ChatHistoryManager.kt:3911` 调用 `TextExporter.exportMultiple`；后者仍用 StringBuilder 拼接全部会话。上游改为分批读写，能减少大导出峰值内存；个人版文件夹、分块正文及导出格式需保留 | 隔离长历史导出，核对内容、取消、进度及峰值内存；相关编译 |
| F03 | ADAPT：消息变体查询规避 SQLite 参数上限 | `7d1b72743` | 当前文件已移至 `chat-storage/.../ChatContentDao.kt`，226 行仍为 `IN (:messageTimestamps)`，361 行入口没有分批。`ChatHistoryManager.hydrateMessages` 使用该入口。上游用时间范围查询再过滤；稀疏时间戳可能多读，应评估分批 IN 是否更适合个人版 | 现有 ChatContentDaoTest，补超大时间戳集合与稀疏集合；保留当前 BLOB 分块正文及 NUL 行为 |
| F04 | ADOPT：Web Chat JS 模块 MIME | `04eaa397f` / PR #1214 | `WebChatHttpBridge` 的资源读取调用 `guessMimeType`；2858 行仍依赖 Android MimeTypeMap，未显式映射 JS/MJS。适合局部加入 Web MIME 映射 | 编译；真实 HTTP 资源响应头及浏览器模块加载 |
| F05 | ADOPT 候选：权限工具描述并发容器 | `5a332577b` | `AIToolHandler.kt:194` 注册描述，`ToolPermissionSystem.kt:199/205/294` 对 mutableMap 写读。改 ConcurrentHashMap 不需要带入上游权限架构；实施前仍需落实个人版并发注册入口，不能仅凭上游 MCP 注释认定已复现 | 跟踪注册线程；保留个人版五档权限、自动审查及审批状态；聚焦注册/读取验证 |
| F06 | ADOPT：保留用户主动清空的默认语音提示词 | `128d4265a` | `CharacterCardManager.kt:1621` 仍用 isNullOrBlank 判断是否补默认值，会把主动保存的空字符串当未初始化；上游按 key 是否存在判断 | 区分缺失 key 与空字符串的迁移用例 |
| F07 | ADAPT：悬浮窗非法尺寸与崩溃状态 | `6d5e8ae82` | `FloatingWindowState` 仍直接 coerceIn，未见有限数检查；小屏幕上下界也需核对。个人版已有 bring-into-view 崩溃处理、桌宠和 service 熔断，不能覆盖整份 service | NaN/Infinity、小窗口、旋转及持久化；Dev 真机正常开关/恢复 |
| F08 | ADAPT：Compose DSL 输入回显与光标 | `1e89d4f2f` / PR #1155 | 个人版已用 TextFieldValue 保持选区，但 externalValue effect 仍可能应用延迟回显；缺少上游新增 ComposeDslTextInputSync 的 echo guard / dispatch queue。需要先复现快速编辑时过期回显，再移植必要部分 | 连续删除、快速输入、异步回显、失焦提交及输入法组合文本 |

## 第二批：有用的新能力，建议单独做

下表均为 `ADAPT` 候选，尚未完成所有运行路径和数据迁移审计。

| 能力 | 上游入口/提交 | 当前差异与取舍 |
| --- | --- | --- |
| 独立 TTS/STT 服务档案 | `9793e816d`，`SpeechServiceProfilesPreferences`、两类 ServiceFactory | 当前只有 SpeechServicesPreferences 单套配置，没有新档案类。适合增加多套语音服务切换，但需迁移已有配置并检查悬浮窗和角色卡调用 |
| 记忆按窗口/日期重建 | `b9e7ec732`、`664d87e17`，ChatMemoryRebuildManager/TimeScope | 当前没有这些管理器。个人版有独立 memory-storage、记忆空间和阅读伴侣，应先定义目标空间、重建范围及避免重复记忆的规则 |
| 摘要分区与对话回顾配置 | `e295a82a8`、`212125304` | 当前 ModelConfigData 未见同名分区/回顾字段。需要兼容个人版上下文管理及 subagent 压缩；不能照搬工具结果截断策略 |
| ToolPkg API 版本、包依赖、加载顺序、运行态 hook、消息菜单 | `5369548f9`、`fce4f6e2e`、`12f614806` | 当前缺少 ToolPkgApiVersion/LoadOrder。上游是一组跨 JS、原生注册、插件桥及 UI 的改动，适合完整独立批次，验证现有阅读伴侣与 v1 subagent 包 |
| Compose DSL 文件选择器、AiChat、侧栏、对话框 | `8ddcf9910`、`91f46fe59`、`fce4f6e2e` | 需要 Kotlin/TS 桥和生成模板同步，不只复制 renderer；依赖前一批 ToolPkg API 的子项应一并评估 |
| 手动数据库/配置健康检查与修复 | `72c1216b9`、`f24144934`、`40081a2a0` | 当前 DataRecovery UI 没有这组 health/repair 入口。个人版存储已经拆分，还要覆盖自身偏好/空间/统计库，不能把上游修复器当通用修复 |
| 应用、插件、终端性能面板 | `62ea0c3ed`、`4e6ae68ae` | 当前无 PerformanceMonitorManager。可用于定位高 CPU/内存，但有采样开销、设备 proc 可读性及 QuickJS 模块接入成本，建议按需要选 |
| DeepSeek Responses 搜索和 reasoning 回放 | `c7b7c995f`、`671659313`、`d566fc774`、`1140f8d54` | 不能当一般 OpenAI Responses 补丁套用。个人版已经分离协议/能力与 provider 路由，须按 DeepSeek 端点语义接入，保留服务端工具记录和历史配对 |
| MiniMax、OpenCode Zen、xAI API 服务入口 | `3c4644ab4`、`76b22b3f6`、`cc2fcdfba` | 当前已有 OpenCode Go、Codex、Grok 账户，但枚举中没有这些上游同名入口；Grok 账户不等于 xAI API。可按需求补入口和模型规则，勿覆盖现有账号登录 |
| 日语翻译、包图标、APK 逆向工具 1.1.0 | `1ed8687d2`、`a7fc01070`、`83acf0755` | 可作为独立可选批次。日语需要保留 Ry 品牌与包身份；逆向工具依赖隔离 JADX 运行时和打包产物，不只改版本号 |

## 不宜直接搬运，或尚需专项证据

| 功能簇 | 筛选结论 |
| --- | --- |
| 并行工具结果顺序 `1ee5dbb48` | 顺序目标已覆盖：个人版 ToolExecutionManager 1410 行以后按原始 invocation 顺序驱动，连续 subagent 并行执行但按序发布。上游同时让保存 XML 与下一轮请求共享格式化结果，这部分仍需专项比较；不把“顺序已覆盖”扩大成整个补丁等价 |
| 跨 provider 工具配对、未匹配结果诊断、group 重复消息、变体事件快照 | `36e370f73`、`2454f4bf9`、`a19d7ef8d`、`923276652`、`2349946b1`、`5ce7d623f`、`567f245ad` 与个人版 callId、invocationIndex、collaboration、turn 权限边界重叠。列入专项 ADAPT 复核，不可整体 cherry-pick |
| 思考内容剥离 ANR `43858cdf5` | 当前 chat-parser/ChatUtils.removeThinkingContent 已改为单次前向扫描，不是上游正则路径。该预编译正则补丁不适用当前实现 |
| 浏览器桌面 viewport、配置列表即时刷新 | 已有个人版 `b81b9b4f8` / `d547f9950`、`d02ffafb7` / `42314842d`；不重复引入对应功能 |
| Token 账本/统计新版 | 个人版有 TokenStatsLedger、spool 及现有恢复体系。上游 `82d70f806` 明确把其尚未发布账本换成 Room；不能将该“未发布数据”假设套到个人版真实已发布数据。保留个人版，价格/缓存命中率/UI 子项另查 |
| Codex OAuth、OpenCode Go、动态思考参数 | 已有对应能力和个人版协议配置。上游模型列表、刷新和参数修复必须逐条比较，不能以同名功能认定缺失或全量等价 |
| Responses 视频转换 `671b80816` | 当前 adapter 没有 video_url/input_video 分支，但只看到缺分支尚不能证明个人版会生成这种输入，且不同端点支持不同。先证明附件生成路径和端点契约，再决定采用；不把上游测试当服务端支持证明 |
| 媒体能力探测 `db60f985e`、`375a3615d` | 三态反馈值得参考；个人版另有 ModelConfigMultimodalCapability 和官方能力目录，须审查结果写回规则，避免探测失败覆盖用户配置 |
| MCP 远程发现后注册 `d3c107dd8` | 上次未采用完整远程 MCP runtime，当前不可仅搬这一末端补丁；先确认拟采用的 transport/runtime |
| 复制预览、多选复制/导出、LaTeX、Markdown 表格链接、编辑器 fling | 可按当前 UI 复现择优采用；上游有多次后续修订，必须取 v1.12.2 最终行为，不能挑早期已被替代的中间提交 |
| ToolPkg manifest 选择/ZipInputStream/隐藏目录、空语音提示之外的偏好迁移 | manifest 修复 `e7fadf0e5`、`f978f906b`、`aa97fff56` 值得随 ToolPkg 批次检查；保留个人版包格式、安装路径和备份边界 |
| 模型历史图片/附件顺序、重试策略、工具结果长度 | 需要按个人版多模态协议和上下文预算分别适配，不能同步统一上限或“清理所有媒体链接”规则 |
| DeepSeek Harness 与侧栏示例 | 可选产品能力，依赖新版 ToolPkg/Compose DSL；不作为修复前置，不替换当前阅读伴侣/协作体系 |
| 在线市场、README 身份、发布版本/CI、原生依赖重设 | SKIP 整体同步。保留个人版市场禁用、Ry 发布、Lint 和 native 锁定链。通用构建修复需按当前 CI 单独比对 |
| 上次 v1.12.1 延后项 | 主题持久化、远程 MCP、memory profile/worldbook 等旧决定继续有效；本轮新增变化若依赖它们，应明确升级为独立项目，不能默认已经采用 |

## 建议执行顺序与验证边界

1. 数据可靠性：F01、F02、F03；先在隔离数据下验证，分别实施，避免混成无法定位的大补丁。
2. 低耦合修复：F04、F06；F05 证明并发路径后加入。
3. 交互稳定性：F07、F08，按真机可复现问题做。
4. 再按需求选择语音档案、ToolPkg、摘要/记忆或 provider 新能力，每类独立适配。

实施时从最新 personal/dev 建隔离 worktree；每批修改后独立只读审计、相关编译/测试，用户可见行为用 Dev 验证。数据修复测试不允许清除用户正式数据。上游 353 提交整体合并不合适。

本轮只有静态代码与 Git/GitHub 元数据分析，没有运行编译、测试、Lint、真机操作或独立审计；没有任何项目被标记为“已实施/验证通过”。新版完成检查点尚未创建。
